const User = require("./user");
const Timetable = require("./timetable");
const Room = require("./room");
const Faculty = require("./faculty");
const Subject = require("./subject");

module.exports = { User, Timetable, Room, Faculty, Subject };
